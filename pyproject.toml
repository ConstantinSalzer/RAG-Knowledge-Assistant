[tool.uv]
managed = true
package = false

[tool.uv.workspace]
members = [
    "services/pipeline",
    "services/backend",
    "services/frontend",
    "services/ingestion",
    "shared",
]

[project]
name = "rag-uni-project"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []